on:
  issue_comment:
    types: [created]

jobs:
  commented:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/github-script@v3
        script: |
          console.log(JSON.stringify(context));
