name: workflow run

on:
  workflow_run:
    workflows:
      - '*'
    types:
      - completed

jobs:
  workflow-run:
    runs-on: ubuntu-latest
    steps:
      - uses: yuya-takeyama/github-actions-metrics-to-datadog-action@initial-implementation
        with:
          datadog-api-key: ${{ secrets.DATADOG_API_KEY }}
