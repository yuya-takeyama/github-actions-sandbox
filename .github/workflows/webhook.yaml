name: webhook

on:
  repository_dispatch:
    types: [foo, bar]

jobs:
  webhook:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/github-script@v7
        with:
          script: |
            console.log(JSON.stringify(context));
