name: matrix

on:
  push

jobs:
  run:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        config:
          - { foo: 'FOO', bar: 'BAR' }
    steps:
      - run: |
          echo "${{ toJSON(matrix) }}"
          echo "${{ toJSON(matrix.config) }}"
          echo "${{ toJSON(matrix.config.foo) }}"
